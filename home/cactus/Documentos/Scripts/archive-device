git archive -9 --format=tar.gz -o ../dashboard/showctrl-20180412.tar.gz release

DEV=$(basename $(pwd)) ; BRANCH=master ; FORMAT=tar.gz ; git archive -9 --format=${FORMAT} -o ../app/devices/${DEV}/${DEV}-$(date +'%Y%m%d').${FORMAT} ${BRANCH}
